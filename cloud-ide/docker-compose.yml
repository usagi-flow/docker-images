version: "3.2"

services:

  cloud-ide:
    build:
      context: .
    container_name: cloud-ide
    volumes:
      - type: bind
        source: ./project
        target: /home/project
    ports:
      - "3000:3000"
    restart: "always"